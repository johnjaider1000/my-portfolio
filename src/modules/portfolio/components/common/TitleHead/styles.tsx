import { styled } from '@mui/material'

export const TitleHeadWrapper = styled('div')`
  display: flex;
  flex-direction: column;

  .before-title {
  }

  .title {
    text-transform: uppercase;
  }
`
